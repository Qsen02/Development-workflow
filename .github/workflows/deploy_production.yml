name: Deploy to production
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout the repo
      uses: actions/checkout@v4
    - name: Deploy
      uses: JorgeLNJunior/render-deploy@v1.4.5
      with:
        service_id: d1aquqbe5dus73duvsb0
        api_key: rnd_fxQthqT8chhQ5rJ83kqj4DKedjjf
